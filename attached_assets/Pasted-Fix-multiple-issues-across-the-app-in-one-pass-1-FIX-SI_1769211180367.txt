Fix multiple issues across the app in one pass:

=== 1. FIX SIGNIN TO ACCEPT USERNAME OR EMAIL ===

FILE: client/src/pages/signin.tsx

Find the email input validation and change it to:
- Remove email format validation entirely
- Accept any text input (username OR email)
- Update placeholder to: "Email or Username"
- Update label to: "Email or Username"

In the signin handler:
- Remove any email validation checks
- Allow "driiva1" to pass through as valid input

=== 2. FIX ROUTING & ADD MISSING ROUTES ===

FILE: client/src/App.tsx

Verify ALL these routes exist:
<Route path="/" element={<HomePage />} />
<Route path="/signin" element={<SignIn />} />
<Route path="/signup" element={<SignUp />} />
<Route path="/onboarding" element={<Onboarding />} />
<Route path="/dashboard" element={<Dashboard />} />
<Route path="/auth-test" element={<AuthTest />} />

If any are missing, add them. Make sure imports are correct.

=== 3. CONSISTENT LAYOUT ACROSS ALL PAGES ===

Apply this wrapper to ALL pages (homepage, signin, signup, onboarding, dashboard):

<div className="min-h-screen bg-gradient-to-br from-gray-900 via-purple-900 to-blue-900">
  {/* page content */}
</div>

Every page should use the SAME gradient background.

=== 4. DARKEN HOMEPAGE ===

FILE: client/src/pages/index.tsx (or welcome.tsx)

Change the background gradient to:
className="min-h-screen bg-gradient-to-br from-black via-gray-900 to-purple-950"

=== 5. FIX LOGO & MOVE EVERYTHING UP ===

On EVERY page that has the Driiva logo:

Change logo container from:
className="flex flex-col items-center mb-8"

To:
className="flex flex-col items-center mb-2 mt-4"

Change logo image from any mb-X to:
className="h-12 w-auto mb-0"

Change title/heading spacing:
className="mt-1 mb-1"

This moves everything 24-32px higher on screen.

=== 6. SIMPLIFY CAROUSEL CONTROLS ===

FILE: client/src/pages/index.tsx (wherever the carousel is)

Replace the three dots indicator with:

<div className="flex items-center justify-center gap-4 mt-6">
  <button
    onClick={previousSlide}
    className="text-white/60 hover:text-white text-2xl transition-colors"
  >
    &lt;
  </button>
  
  <div className="w-2 h-2 rounded-full bg-cyan-400"></div>
  
  <button
    onClick={nextSlide}
    className="text-white/60 hover:text-white text-2xl transition-colors"
  >
    &gt;
  </button>
</div>

Add previousSlide and nextSlide functions that change the carousel index.

=== 7. STYLE "SEE DRIIVA" BUTTON ===

FILE: client/src/pages/index.tsx

Replace the "See Driiva" button with:

<button
  onClick={launchDemoMode}
  className="relative w-full max-w-md px-8 py-4 text-lg font-semibold text-white rounded-xl overflow-hidden group"
  style={{
    background: 'linear-gradient(135deg, rgba(6, 182, 212, 0.3), rgba(59, 130, 246, 0.3), rgba(6, 182, 212, 0.3))',
    backdropFilter: 'blur(10px)',
    border: '1px solid rgba(6, 182, 212, 0.4)',
    boxShadow: '0 8px 32px rgba(6, 182, 212, 0.2)',
  }}
>
  <span className="relative z-10">See Driiva</span>
  <div
    className="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500"
    style={{
      background: 'linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent)',
      animation: 'shimmer 2s infinite',
    }}
  ></div>
</button>

Add this to your global CSS (client/src/index.css):

@keyframes shimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

=== MAKE ALL THESE CHANGES NOW ===
