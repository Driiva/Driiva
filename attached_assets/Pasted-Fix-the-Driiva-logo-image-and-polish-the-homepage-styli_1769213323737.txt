Fix the Driiva logo image and polish the homepage styling.

=== FIX LOGO IMAGE ===

Option A - If logo PNG has padding/dead space:

1. Open the logo image in any image editor
2. Crop tight to the text/icon with no padding
3. Re-export as PNG with transparent background
4. Replace in public/ or assets/ folder

Option B - CSS fix if you can't edit the image:

In client/src/components/DriivaLogo.tsx, update:

<img 
  src="/driiva-logo.png"
  alt="Driiva"
  className={`${sizeClasses[size]} w-auto`}
  style={{ 
    objectFit: 'contain',
    // Crop dead space with clip-path if needed:
    clipPath: 'inset(10% 5% 10% 5%)',  // Adjust percentages
    transform: 'scale(1.2)',  // Scale up to compensate
  }}
/>

=== POLISH HOMEPAGE ===

FILE: client/src/pages/index.tsx (or welcome.tsx)

1. Use darker gradient:
className="min-h-screen bg-gradient-to-br from-[#050510] via-[#0a0a20] to-[#0a1525]"

2. DriivaLogo with size xl:
<DriivaLogo size="xl" />

3. Tighten spacing between logo and tagline:
<div className="flex flex-col items-center gap-1">
  <DriivaLogo size="xl" />
  <h1 className="text-xl font-bold text-white mt-0">AI-powered, community-driven</h1>
  <p className="text-cyan-400 text-sm">Your driving, rewarded.</p>
</div>

4. "See Driiva" button with shimmer:

<button
  onClick={launchDemoMode}
  className="relative w-full max-w-xs px-8 py-4 text-lg font-semibold text-white rounded-xl overflow-hidden group transition-all duration-300 hover:scale-105"
  style={{
    background: 'linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(59, 130, 246, 0.3))',
    backdropFilter: 'blur(12px)',
    border: '1px solid rgba(6, 182, 212, 0.3)',
    boxShadow: '0 0 30px rgba(6, 182, 212, 0.15), inset 0 1px 0 rgba(255,255,255,0.1)',
  }}
>
  <span className="relative z-10">See Driiva</span>
  <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
</button>

5. Make sure there's NO nav bar on homepage - only "See Driiva" and "I Already Have an Account"

Apply these changes now.
