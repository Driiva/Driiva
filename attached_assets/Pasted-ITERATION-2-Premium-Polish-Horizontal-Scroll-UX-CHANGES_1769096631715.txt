ITERATION 2: Premium Polish + Horizontal Scroll UX

CHANGES FROM V1:

1. REMOVE GREEN ICON ELEMENT:
   - Delete the lightning bolt circle icon entirely
   - This was the teal/green rounded square with bolt

2. HEADER LOGO REPLACEMENT:
   - Use generated-image-3.jpg as primary header logo
   - Size: 280px width (hero treatment, not small icon)
   - Position: Center-aligned, 48px from top of safe area
   - High-res: Use 2x scaling for Retina displays
   - Margin below logo: 24px before tagline

3. HORIZONTAL SCROLL CARDS:
   - Convert from vertical stack to single horizontal scrollable container
   - Container specs:
     • Display: flex with horizontal scroll (overflow-x: scroll)
     • Scroll snap: snap-x mandatory with snap-center on cards
     • Hide scrollbar: -webkit-scrollbar { display: none }
     • Padding: 0 24px (edge cards peek for scroll affordance)
     • Gap between cards: 16px
   
   - Individual card sizing:
     • Width: 320px (fixed, allows 1.2 cards visible to suggest scroll)
     • Height: 180px
     • Flex-shrink: 0 (prevents compression)
     • Each card maintains icon + title + description layout

4. REDUCED CARD OPACITY:
   - Glass-morphism adjustments:
     • background: rgba(255, 255, 255, 0.06) — down from 0.12
     • backdrop-filter: blur(20px) — increased blur
     • border: 1px solid rgba(255, 255, 255, 0.10) — down from 0.18
     • box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08) — softer shadow
   - Result: More transparent, subtle glass effect

5. BACKGROUND ENHANCEMENTS:

   A. Soft Radial Blur:
      - Apply CSS filter to background image container:
        • filter: blur(3px)
      - Add radial gradient overlay for vignette:
        • background: radial-gradient(circle at center, 
          transparent 0%, 
          rgba(0, 0, 0, 0.2) 100%)
      - This creates soft focus with darker edges

   B. Subtle Dot Animation:
      - Create floating dot particles (8-12 dots)
      - Dot specs:
        • Size: 4-8px diameter circles
        • Color: rgba(0, 217, 160, 0.15) — brand teal, very subtle
        • Position: Absolute, random placement
        • Animation: Slow vertical float (20-40s duration each)
        • Use transform: translateY() for performance
        • Stagger animation delays for organic feel
      - Dots layer: Between blurred background and content (z-index: 1)

6. CONTRAST REDUCTION:
   - Apply to entire page container:
     • filter: contrast(0.92) brightness(0.98)
   - This creates the premium muted aesthetic
   - Text remains legible due to existing white/high-contrast typography

7. PRESERVE:
   - All button functionality and styling
   - Tagline text and positioning
   - All existing touch handlers
   - Feature card icons (chart, wallet, trophy) and text content
   - CTA buttons ("Get Started" / "I Already Have an Account")

LAYOUT FLOW (TOP TO BOTTOM):
1. Driiva logo (generated-image-3.jpg) — LARGE, hero size
2. Tagline text
3. Horizontal scrollable card container (single row)
4. Spacer (40px)
5. CTA buttons (stacked)

PERFORMANCE NOTES:
- Use CSS animations (not JS) for dot movement
- will-change: transform on animated elements
- Optimize horizontal scroll with scroll-snap for native feel
- Test scroll momentum on iOS Safari

DELIVERABLE:
Drop-in replacement that elevates brand perception while improving feature discoverability through horizontal scroll pattern.
